# Range Guardian - copy to .env and fill in. NEVER commit your real .env.

# Telegram bot (Day 2) - get a token from @BotFather
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# SECURITY NOTE
# This project NEVER reads or stores your Solana private key.
# Execution (rebalance) is performed by byreal-cli using a keypair YOU set locally
# via:  byreal-cli wallet set --private-key "<key>"   (stored under ~/.config/byreal)
# Monitoring is fully read-only and needs no key.
